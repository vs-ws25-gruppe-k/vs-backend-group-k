services:
  postgres_prod:
    container_name: 'postgres-tododb-prod'
    image: 'postgres:18.1-trixie'
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
    ports:
      - '${POSTGRES_PORT}:5432'